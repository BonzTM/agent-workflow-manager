{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear",
        "hooks": [
          {
            "type": "command",
            "command": "bash -lc 'dir=\"$PWD\"; while [ \"$dir\" != \"/\" ] && [ ! -d \"$dir/.codex/hooks\" ]; do dir=$(dirname \"$dir\"); done; if [ ! -x \"$dir/.codex/hooks/awm-session-context.sh\" ]; then exit 0; fi; exec \"$dir/.codex/hooks/awm-session-context.sh\"'",
            "statusMessage": "injecting AWM session context"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash -lc 'dir=\"$PWD\"; while [ \"$dir\" != \"/\" ] && [ ! -d \"$dir/.codex/hooks\" ]; do dir=$(dirname \"$dir\"); done; if [ ! -x \"$dir/.codex/hooks/awm-prompt-guard.sh\" ]; then exit 0; fi; exec \"$dir/.codex/hooks/awm-prompt-guard.sh\"'",
            "statusMessage": "checking AWM prompt flow"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash -lc 'dir=\"$PWD\"; while [ \"$dir\" != \"/\" ] && [ ! -d \"$dir/.codex/hooks\" ]; do dir=$(dirname \"$dir\"); done; if [ ! -x \"$dir/.codex/hooks/awm-stop-guard.sh\" ]; then exit 0; fi; exec \"$dir/.codex/hooks/awm-stop-guard.sh\"'",
            "statusMessage": "checking AWM closeout reminder"
          }
        ]
      }
    ]
  }
}
